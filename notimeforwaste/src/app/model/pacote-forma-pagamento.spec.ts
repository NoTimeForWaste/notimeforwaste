import { PacoteFormaPagamento } from './pacote-forma-pagamento';

describe('PacoteFormaPagamento', () => {
  it('should create an instance', () => {
    expect(new PacoteFormaPagamento()).toBeTruthy();
  });
});
