import { PacoteFormaEntrega } from './pacote-forma-entrega';

describe('PacoteFormaEntrega', () => {
  it('should create an instance', () => {
    expect(new PacoteFormaEntrega()).toBeTruthy();
  });
});
